(function() {
  var NodeApplication;
  module.exports = NodeApplication = (function() {
    function NodeApplication(configuration, root) {
      this.configuration = configuration;
      this.root = root;
      console.log("Node application found at " + this.root + ". Not yet implemented.");
    }
    return NodeApplication;
  })();
}).call(this);
